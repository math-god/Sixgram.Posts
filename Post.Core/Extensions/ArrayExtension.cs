namespace Post.Core.Extensions;

public static class ArrayExtension
{
    /*public static Array Add(this Array array, Guid newItem)
    {
        Array newArray;

        foreach (var item in array)
        {
            newArray
        }
    }*/
}